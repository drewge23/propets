import React from 'react';

function Loading(props) {
    return (
        <div style={{
            height: '300px',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
        }}>
            <h3>Loading...</h3>
        </div>
    );
}

export default Loading;